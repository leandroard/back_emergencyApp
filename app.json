{
  "scripts": {
    "dokku": {
      "predeploy": "python manage.py compilemessages && python manage.py collectstatic --noinput",
      "postdeploy": "echo \"Environment: $ENVIRONMENT\" && python manage.py migrate && ./manage.py loaddata initial"
    }
  }
}
